# Frontend API Configuration
# API base URL for the backend
# In production, this will use /api (via nginx proxy)
# In development, set this to your backend URL (default: http://localhost:8001)
VITE_API_URL=http://localhost:8001
